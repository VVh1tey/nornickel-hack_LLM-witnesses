# Скопируй в .env и заполни. .env в .gitignore — ключи в git не попадают.
YC_FOLDER_ID=b1g...
YC_API_KEY=AQVN...
# модель для генерации/экстракции: pro, не lite — качество экстракции определяет граф
YC_MODEL=yandexgpt
EMBEDDING_MODEL=BAAI/bge-m3
LLM_MAX_CONCURRENCY=4
